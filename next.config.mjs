/** @type {import('next').NextConfig} */
const nextConfig = {images: {
    dangerouslyAllowSVG: true, // Enable SVG rendering
    domains: [], // Add any external domains for images, if needed
  },};

export default nextConfig;
